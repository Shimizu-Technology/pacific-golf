# Stripe configuration
# The API key is loaded from Settings (database) or environment variables
# We'll set Stripe.api_key dynamically in controllers when needed
# because the key is stored in the database (Settings table)

Stripe.api_version = "2024-11-20.acacia"

